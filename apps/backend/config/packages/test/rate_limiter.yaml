framework:
    rate_limiter:
        login:
            policy: 'fixed_window'
            limit: 100
            interval: '1 minute'

        registration:
            policy: 'fixed_window'
            limit: 100
            interval: '1 minute'

        forgot_password:
            policy: 'fixed_window'
            limit: 100
            interval: '1 minute'
